module Main (main) where

    {----------------------------------------------------------------------
              Módulo en el que se realizarán las comprobaciones
            necesarias para saber si todo funciona correctamente.
    ----------------------------------------------------------------------}

import Test.QuickCheck
import HUnit

main :: IO ()
main = putStrLn "Conjunto de pruebas aún no implementado."

